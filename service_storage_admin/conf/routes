# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Setup endpoints
GET     /v1/unit/:id                    no.uio.musit.microservice.storageAdmin.resource.StorageUnitResource.getSubNodes(id:Long)
POST    /v1/unit                        no.uio.musit.microservice.storageAdmin.resource.StorageUnitResource.postRoot
