# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Setup endpoints
GET     /v1/now                    no.uio.musit.microservices.time.resource.TimeResource.now(filter: Option[no.uio.musit.microservices.common.domain.MusitFilter], search: Option[no.uio.musit.microservices.common.domain.MusitSearch])
