# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Setup endpoints
GET     /v1/now                    controllers.TimeController_V1.now(filter: Option[String])
