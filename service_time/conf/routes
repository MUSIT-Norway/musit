# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Setup endpoints
GET     /v1/now                    no.uio.musit.microservice.time.resource.TimeResource_V1.actionGetNow
