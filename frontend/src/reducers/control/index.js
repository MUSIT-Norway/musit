const initialState = {
  data: {
    temperatureControl: {
      ok: true
    },
    relativeHumidity: {
      ok: false
    }
  }
};

const authReducer = (state = initialState) => {
  return state;
}

export default authReducer
